# AUDIT DB Monolith vs Tenants — 2026-04-20

> Rapport read-only. Aucune modification effectuée.
> Source : 3 DBs SQLite sur le VPS (`/var/www/planora-data/`).

---

## 1. Inventaire

| DB | Path | Taille | Tables | Indexes | Integrity |
|---|---|---|---|---|---|
| **monolithe** | `calendar360.db` | 5.3 MB | **95** | 179 | OK |
| **CapFinances** | `tenants/c1776169036725.db` | 1.6 MB | **89** | 172 | OK |
| **MonBilan** | `tenants/c-monbilan.db` | 3.5 MB | **89** | 172 | OK |
| Control Tower | `control_tower.db` | 110 KB | (out of scope) | — | OK |

⚠ **`PRAGMA foreign_keys = 0`** sur les 3 DBs — les déclarations FK sont **décoratives** (jamais appliquées au runtime).

## 2. Différences de tables

### Tables présentes UNIQUEMENT dans monolithe (6)
| Table | Rows | Verdict |
|---|---|---|
| `phone_plans` | 4 | ✅ Globale by-design (catalogue marketplace) |
| `sms_packs` | 4 | ✅ Globale by-design |
| `voip_packs` | 4 | ✅ Globale by-design |
| `supra_admins` | 1 | ✅ Globale by-design (CLAUDE.md §0) |
| `wa_verifications` | 0 | ✅ Globale by-design (codes OTP WhatsApp) |
| `sessions` | 75 | 🟡 **AMBIGU** : sessions par companyId, mais reste pratique en monolithe pour supra-impersonation |

→ **Aucune action** : ces 6 tables ne doivent PAS être propagées dans les tenants.

### Tables EN PLUS dans tenants
**Aucune.** Les 89 tables des tenants sont toutes présentes dans le monolithe.

### Tables divergentes structurellement (4)
**Identiques dans CapFinances + MonBilan (les 2 tenants sont en sync)** mais divergent du monolithe.

| Table | Type de diff | Détails |
|---|---|---|
| **`audit_logs`** | Triggers manquants | Tenants n'ont PAS les 2 triggers `prevent_audit_update` + `prevent_audit_delete` (protection immutabilité) |
| **`bookings`** | 8 colonnes manquantes | `bookedByCollaboratorId`, `meetingCollaboratorId`, `agendaOwnerId`, `bookingType` (default `'external'`), `bookingOutcome`, `bookingOutcomeNote`, `bookingOutcomeAt`, `transferMode` |
| **`collaborators`** | 4 colonnes manquantes | `acceptInternalMeetings` (default 1), `shareAgendaAvailability` (default 1), `autoAcceptMeetings` (default 0), `meetingPriorityLevel` (default 1) |
| **`contacts`** | 8 colonnes manquantes | `ownerCollaboratorId`, `executorCollaboratorId`, `meetingCollaboratorId`, `followMode` (default `'owner_only'`), `visibilityScope` (default `'owner'`), `lastMeetingOutcome`, `lastMeetingDate`, `lastMeetingCollaboratorId` |

### Indexes manquants dans tenants (6, cohérent avec colonnes manquantes)
- `idx_bookings_agenda_owner`, `idx_bookings_meeting_collab`, `idx_bookings_type`
- `idx_contacts_executor`, `idx_contacts_meeting_collab`, `idx_contacts_owner`

→ Ces indexes seront naturellement à créer en même temps que les colonnes correspondantes.

## 3. État des données (orphelins + doublons)

### CapFinances — ✅ PARFAITEMENT PROPRE
| Check | Result |
|---|---|
| bookings sans collab/contact/calendar valide | **0 / 0 / 0** |
| contacts assignés à collab inexistant | **0** |
| contact_followers orphelins | **0 / 0** |
| call_logs orphelins | **0 / 0** |
| Doublons emails / phones | **0 / 0** |

Totaux : 2 collabs, 45 contacts, 1 booking, 0 followers, 6 call_logs.

### MonBilan — 🟡 PRESQUE PROPRE
| Check | Result |
|---|---|
| **bookings sans contact valide** | **30 / 39** ⚠ |
| Autres orphelins | 0 partout |
| Doublons | 0 |

Totaux : 4 collabs, 13 contacts, 39 bookings, 0 followers, 142 call_logs.
→ **À investiguer** : les 30 bookings dont le `contactId` ne pointe sur aucun contact. Cause probable : RDV publics (Calendly-style) où le visiteur n'est pas dans la CRM (donc contactId fictif ou contact supprimé après).

### Monolithe — ⚠ POLLUTION HISTORIQUE
| Check | Result |
|---|---|
| **bookings sans contact valide** | **32 / 48** ⚠ |
| **contacts assignés à collab inexistant** | **2** ⚠ |
| **call_logs sans contact valide** | **48 / 227** (21%) ⚠ |
| **Doublons emails dans contacts** | **13** ⚠ |
| **Doublons phones dans contacts** | **9** ⚠ |

Totaux : 12 collabs, 287 contacts, 48 bookings, 6 followers, 227 call_logs.
→ Le monolithe a accumulé de la dette : il hébergeait toutes les companies avant la mise en place de l'isolation par tenant.

## 4. Source de vérité — recommandation

**Le monolithe est la source de vérité STRUCTURELLE** (schéma le plus à jour, V7 complet).

→ Les tenants doivent être **alignés sur le monolithe** :
1. Ajouter les 4 colonnes à `collaborators`
2. Ajouter les 8 colonnes à `bookings` + 3 indexes
3. Ajouter les 8 colonnes à `contacts` + 3 indexes
4. Ajouter les 2 triggers à `audit_logs`

Cependant, les **données** du monolithe sont moins propres que celles des tenants — donc la VÉRITÉ DONNÉES diffère par scope :
- Pour CapFinances / MonBilan : la vérité est dans leur tenant (déjà migré)
- Pour les autres companies : reste dans le monolithe (héritage à phaser out)

## 5. Plan de correction recommandé

### Phase A — Structurelle (non destructive, 0 risque)
**ALTER TABLE ADD COLUMN** dans les 2 tenants pour ajouter les 20 colonnes manquantes + 6 indexes + 2 triggers. Toutes les colonnes ont des `DEFAULT` donc aucune ligne existante n'est cassée.

Script : `propagate-schema-monolith-to-tenants.sql` (à générer)
Backup auto VPS avant exécution.

### Phase B — Nettoyage data MonBilan (low risk)
1. Investiguer les 30 bookings sans contact :
   - Si visiteur public (pas de contact CRM voulu) → laisser, c'est by-design
   - Si contact supprimé → soit recréer un contact placeholder, soit set `contactId = ''`
2. Décision après inspection user.

### Phase C — Nettoyage data Monolithe (cleanup historique, 13 doublons emails à dédupliquer)
1. **Lister les 13 doublons emails + 9 doublons phones** avec les détails (companyId, dates, qui a créé)
2. Pour chaque doublon : décider à la main (merge ou delete) — pas de fix auto
3. Pour les 32 bookings + 48 call_logs orphelins : idem, audit puis décision

### Phase D — Activation FKs (après nettoyage)
1. Pour chaque DB :
   - Ajouter `PRAGMA foreign_keys = ON;` dans le code de connexion `database.js` + `tenantResolver.js`
   - Vérifier qu'aucune contrainte FK n'est violée par les données restantes
2. Si violation → revenir en C
3. Sinon → activer définitivement

### Phase E — Test différentiel + monitoring
1. Après chaque phase, smoke test prod (`./v7-deploy/deploy.sh --check`)
2. Re-audit post-correction (le script d'audit doit être réutilisable)

## 6. Sécurisation

- **Backup AVANT chaque phase** : tarball compressé des 3 DBs, stocké VPS + Mac avec SHA256
- **Modifications en TRANSACTION** : `BEGIN; ALTER TABLE ...; COMMIT;` — rollback automatique si erreur
- **`PRAGMA integrity_check`** après chaque modification
- **Re-audit** après chaque phase (le script est read-only)

## 7. Décisions à prendre par MH

Avant que je code/exécute la moindre modif, valider :

1. **Schéma cible** : confirmer que monolithe = source de vérité structurelle (les 20 colonnes + 6 indexes + 2 triggers à propager)
2. **Sessions** : laisser dans monolithe seul (recommandé) ou migrer dans tenants ?
3. **30 bookings MonBilan sans contact** : laisser tel quel (RDV publics) ou nettoyer ?
4. **13 doublons emails monolithe** : audit user puis décision case-by-case (je donne la liste)
5. **48 call_logs monolithe orphelins** : delete (perdre l'historique d'appel) ou laisser (rejet par futur FK) ?
6. **Activation FKs** : on l'active après nettoyage, ou on reste en `foreign_keys=0` (statu quo actuel) ?

---

## ANNEXES

### Liste exacte des colonnes à ajouter

#### `bookings` (8 colonnes)
```sql
ALTER TABLE bookings ADD COLUMN bookedByCollaboratorId TEXT DEFAULT '';
ALTER TABLE bookings ADD COLUMN meetingCollaboratorId  TEXT DEFAULT '';
ALTER TABLE bookings ADD COLUMN agendaOwnerId          TEXT DEFAULT '';
ALTER TABLE bookings ADD COLUMN bookingType            TEXT DEFAULT 'external';
ALTER TABLE bookings ADD COLUMN bookingOutcome         TEXT DEFAULT '';
ALTER TABLE bookings ADD COLUMN bookingOutcomeNote     TEXT DEFAULT '';
ALTER TABLE bookings ADD COLUMN bookingOutcomeAt       TEXT DEFAULT '';
ALTER TABLE bookings ADD COLUMN transferMode           TEXT DEFAULT '';

CREATE INDEX IF NOT EXISTS idx_bookings_agenda_owner   ON bookings(agendaOwnerId);
CREATE INDEX IF NOT EXISTS idx_bookings_meeting_collab ON bookings(meetingCollaboratorId);
CREATE INDEX IF NOT EXISTS idx_bookings_type           ON bookings(bookingType);
```

#### `collaborators` (4 colonnes)
```sql
ALTER TABLE collaborators ADD COLUMN acceptInternalMeetings  INTEGER DEFAULT 1;
ALTER TABLE collaborators ADD COLUMN shareAgendaAvailability INTEGER DEFAULT 1;
ALTER TABLE collaborators ADD COLUMN autoAcceptMeetings      INTEGER DEFAULT 0;
ALTER TABLE collaborators ADD COLUMN meetingPriorityLevel    INTEGER DEFAULT 1;
```

#### `contacts` (8 colonnes)
```sql
ALTER TABLE contacts ADD COLUMN ownerCollaboratorId        TEXT DEFAULT '';
ALTER TABLE contacts ADD COLUMN executorCollaboratorId     TEXT DEFAULT '';
ALTER TABLE contacts ADD COLUMN meetingCollaboratorId      TEXT DEFAULT '';
ALTER TABLE contacts ADD COLUMN followMode                 TEXT DEFAULT 'owner_only';
ALTER TABLE contacts ADD COLUMN visibilityScope            TEXT DEFAULT 'owner';
ALTER TABLE contacts ADD COLUMN lastMeetingOutcome         TEXT DEFAULT '';
ALTER TABLE contacts ADD COLUMN lastMeetingDate            TEXT DEFAULT '';
ALTER TABLE contacts ADD COLUMN lastMeetingCollaboratorId  TEXT DEFAULT '';

CREATE INDEX IF NOT EXISTS idx_contacts_executor       ON contacts(executorCollaboratorId);
CREATE INDEX IF NOT EXISTS idx_contacts_meeting_collab ON contacts(meetingCollaboratorId);
CREATE INDEX IF NOT EXISTS idx_contacts_owner          ON contacts(ownerCollaboratorId);
```

#### `audit_logs` (2 triggers)
```sql
CREATE TRIGGER IF NOT EXISTS prevent_audit_update BEFORE UPDATE ON audit_logs
  BEGIN SELECT RAISE(ABORT, 'audit_logs is immutable'); END;
CREATE TRIGGER IF NOT EXISTS prevent_audit_delete BEFORE DELETE ON audit_logs
  BEGIN SELECT RAISE(ABORT, 'audit_logs is immutable'); END;
```

**Total : 20 colonnes + 6 indexes + 2 triggers à appliquer sur 2 DBs (CapFinances + MonBilan).**
