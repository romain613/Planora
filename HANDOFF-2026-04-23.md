# HANDOFF 2026-04-23 — Deploy post-S2.10

> Session : S1 cleanup + S2.1 → S2.10 (10/13 modals extraits)
> Deploy + triple-backup + smoke test validés, prêt pour reprise S2.11.

---

## 1. État final — commits clean-main

63 commits pushés sur `origin/clean-main` depuis `origin/main`. Points clés :

### Cleanup post-S1 (4 commits)
| Hash | Description |
|---|---|
| `ae29107d` | Cleanup post-S1 (1/4) — extract FicheStatusHistory → fixes useState latent |
| `7d234a21` | Cleanup post-S1 (2/4) — remove typeof X !== undefined in S1 scope |
| `2d71fba7` | Cleanup post-S1 (3/4) — clean dead destructures + dead imports in CrmTab |
| `8f2e1e37` | Cleanup post-S1 (4/4) — extract FicheShare (Partage tab) |

### S2 Modals extraction (10/13)
| Wave | Hash | Modal | Δ CollabPortal |
|---|---|---|---:|
| A 1/5 | `f06648f0` | CockpitFloatingButton | −4 |
| A 2/5 | `1551b433` | CockpitMinimizedButton | −5 |
| A 3/5 | `051399ad` | NotificationsDropdown | −40 |
| A 4/5 | `cfdb92ee` | V7TransferModal | −25 |
| A 5/5 | `8855d732` | ContractModal | −38 |
| B 1/3 | `9fc4d965` | IncomingCallBanner | −9 |
| B 2/3 | `b0c2c90d` | NewContactModal | −70 |
| B 3/3 | `6131fa6a` | IaProactiveWidget | −17 |
| C 1/5 | `7deaa7d6` | NrpPostCallModal | −131 |
| C 2/5 | `1fc23f53` | BookingDetailModal | −192 |

**Tip HEAD clean-main** : `1fc23f53`

### Métriques fichiers

| Fichier | Début session | Fin session |
|---|---:|---:|
| CollabPortal.jsx | 6211 l | **5680 l** (−531) |
| CrmTab.jsx | 54 l | 54 l (inchangé) |
| FicheContactModal.jsx | 422 l | 358 l (−64 via cleanup) |
| Dossier modals/ | — | 10 fichiers, ~1028 l |

---

## 2. État production

### Bundle frontend
- **Déployé** : `index-CRM8NrBO.js` + `index-DydZeC02.css`
- **Emplacement** : `/var/www/vhosts/calendar360.fr/httpdocs/`
- **Taille** : 2987 kB JS (gzip 668 kB), 0.12 kB CSS

### Backend
- **PM2** : PID 291139, `calendar360` online, uptime depuis deploy
- **Healthcheck** : `GET /api/health` → `{"status":"ok","db":"connected","companies":6,"collaborateurs":13}`
- **DB_PATH** : `/var/www/planora-data/calendar360.db` (monolithe officiel, guard E.3.8-E actif)

### Smoke test prod
```
Target: https://calendar360.fr/
Rounds: 1, Events: 0, Critical: 0, Console errors: 0
Title: Calendar360 — par COMPETENCES FIRST
```

---

## 3. Backup triple-redondance

**Timestamp** : `20260423-002146` (UTC)
**Taille** : 402 Mo (gzip)
**SHA-256** : `01b0ec4bec5acd1992328a4ba4bc3406ceb67c62b9c4ad1c4152064c37896fd5`
**Identique sur les 3 copies** ✓

| Localisation | Chemin | Permissions |
|---|---|---|
| VPS | `/var/backups/planora/planora-backup-20260423-002146.tar.gz` | `-rw-------` (root) |
| Mac | `/Users/design/Desktop/PLANORA/backups/planora-backup-20260423-002146.tar.gz` | `-rw-------` (design) |
| iCloud | `~/Library/Mobile Documents/com~apple~CloudDocs/PLANORA-backups/...` | `-rw-------` (design) |

Contenu : `planora/` (code source + app/dist exclu) + `planora-data/` (DBs + storage, `backups/` exclu).

---

## 4. Merge clean-main → main — NON EFFECTUÉ

**Statut** : bloqué par policy sandbox Claude Code (direct push + PR creation denied pour la branche par défaut).

**Action requise MH** : merger manuellement via GitHub UI ou CLI local :
- GitHub web UI : créer PR `clean-main` → `main` sur https://github.com/romain613/Planora/
- OU en local :
  ```bash
  git checkout main
  git pull origin main
  git merge --ff-only clean-main
  git push origin main
  ```

Code déjà pushé sur `origin/clean-main` — rien à refaire côté git avant le merge.

---

## 5. État session S2

**Progression S2 : 10/13 modals extraits (77 %)**

### Restants (Wave C, 3/5)
| # | Modal | Estim. lignes | Complexité |
|---|---|---:|---|
| S2.11 | ScheduleRdvModal | ~192 | 🔴 Haute (picker créneaux busy bookings + GCal, catégories RDV, switch contact existant/nouveau) |
| S2.12 | CsvImportModal | ~317 | 🔴 Haute (FileReader parsing, duplicates DB, création batch contact_fields) |
| S2.13 | CockpitWindow | ~181 | 🔴 Boss final (VoIP live, transcription, coaching, notes, 3 colonnes) |

**Prochaine session** : lancer S2.11 ScheduleRdvModal sur validation MH.

### Dette identifiée pendant S2 (préservée verbatim)
- **Bug latent `collaborators` non déclaré** dans BookingDetailModal (onClick Confirmer reschedule L~118) — ReferenceError si clic. Préexistant, signalé dans commit S2.10.
- **Fantôme states** : `pipelineRdvModal`, `contactAnalysesHistoryModal` destructurés dans FicheContactModal mais JSX jamais émis. Candidats cleanup dédié post-S2.

---

## 6. Points d'attention pour la reprise

### Conditions de lancement S2.11
1. Test manuel MH sur prod : ouvrir 1 booking depuis Agenda → valider statut change, replanification, notifier, delete ; ouvrir fiche client → valider navigation vers CRM.
2. Validation NRP post-call : provoquer un NRP (appel sans réponse), vérifier modal ouvre + incrémentation compteur + historique.
3. Validation V7 transfer : transférer un contact depuis Pipeline Live.
4. Validation sticky bar : bouton Nouveau Contact → modal ouvre, création OK.

### Commandes utiles
```bash
# Smoke prod
cd /Users/design/Desktop/PLANORA/ops/smoke && npm run smoke

# Audit orphelins
node /Users/design/Desktop/PLANORA/ops/smoke/audit-collab-orphans.mjs | jq .summary

# VPS logs live
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 "pm2 logs calendar360 --lines 50 --nostream --err"

# Rollback bundle si régression (ancien bundle prod : index-BwKLvCj7.js, copié avant deploy)
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 "cp /var/www/vhosts/calendar360.fr/httpdocs/assets/index-BwKLvCj7.js ... [pas copié avant, rollback via backup]"
```

### Rollback en cas de régression
- `git revert 1fc23f53` (S2.10) ou plus loin selon la gravité
- Redeploy `npm run build` + rsync
- Alternative extrême : restaurer backup `20260423-002146.tar.gz` via `tar xzf` dans `/var/www/`

### Dépendance bloquante
- PR `clean-main` → `main` à valider par MH (via GitHub UI ou CLI manuel) pour aligner `origin/main` sur le tip actuel.

---

## 7. Checklist clôture session

| Item | Statut |
|---|:---:|
| Build local OK | ✅ |
| Scan A/B Grand/CrmTab verts | ✅ |
| Push clean-main → origin | ✅ |
| Deploy VPS (httpdocs + pm2 restart) | ✅ |
| Healthcheck backend | ✅ |
| Smoke test prod (calendar360.fr) | ✅ |
| Merge clean-main → main | ⏸ MH à faire manuellement |
| Triple backup (VPS + Mac + iCloud) | ✅ |
| SHA-256 vérifié identique | ✅ |
| chmod 600 sur les 3 copies | ✅ |
| HANDOFF rédigé | ✅ (ce document) |

**Session prête pour reprise — S2.11 ScheduleRdvModal.**
