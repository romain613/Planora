# HANDOFF — Refactor Frontend PLANORA (état au 2026-04-19, soir)

> Document de reprise de session. Tout ce qu'il faut pour continuer le refactor
> sans repartir de zéro.
>
> **Lire d'abord** : [CLAUDE.md](CLAUDE.md) (règles projet + infrastructure).
> **Ce fichier** : focus sur le refactor frontend + état courant.

---

## 0. CONTEXT EN 30 SECONDES

- Application : **Calendar360 / PLANORA** — SaaS CRM/Pipeline/Agenda/VoIP
- Propriétaire : MH (rc.sitbon@gmail.com)
- Frontend était un monolithe React de **39 338 lignes** dans un seul `App.jsx`
- 7 sessions de refactor → **22 commits** (Phases 0 → 14b)
- État actuel : App.jsx **605 lignes** (orchestrateur), CollabPortal **5 832 lignes** (−69 %), AdminDash **11 775 lignes**, code réparti en **59 fichiers modulaires**
- Prod : https://calendar360.fr/ — HTTP 200, hash `index-DdfWNvyx.js` (Phase 14b PhoneTab déployée 2026-04-20)
- 9 tabs collab extraits : Messages, Tables, AI Profile, Availability, Objectifs, Home, Agenda, **CRM, Phone (mastodon)**
- Branche git : `clean-main` ⇔ `origin/main` sur https://github.com/romain613/Planora
- **Tabs collab extraits** : Messages, Tables, AI Profile, Availability, Objectifs (5 sur 12+)
- **Reste à faire** : Settings (à sub-découper), Home, Agenda, CRM, Phone — voir §3
- **Règle absolue** : ne JAMAIS rajouter de code dans App.jsx, structurer dans la bonne hiérarchie (cf. §4)

---

## 1. ÉTAT ACTUEL

### Git
```
HEAD local   = 114b7831 (pushed to origin/main)
Branche      = clean-main
Commits refactor = 18 (du 7fe0e80c au 114b7831)
```

### VPS
```
Serveur      = root@136.144.204.115 (SSH clé: ~/.ssh/id_ed25519)
Code source  = /var/www/planora/app/src/
App.jsx      = 605 lignes (orchestrateur)
CollabPortal = 15 923 lignes
AdminDash    = 11 775 lignes
Hash prod    = index-DsvuN8GF.js (2 881 KB minifié, 637 KB gzip)
pm2          = calendar360 online (post-reboot 2026-04-20)
httpdocs     = /var/www/vhosts/calendar360.fr/httpdocs/

Note 2026-04-20 : VPS rebooté manuellement par MH suite à panne de services
(nginx + pm2 inaccessibles). HTTPS répondait avec ERR_CONNECTION_CLOSED
malgré ports 80/443 ouverts. Reboot a tout remis en place. Cause exacte
non investiguée — probablement OOM kill ou crash de service pendant la nuit.
```

### Local Mac
```
Projet       = /Users/design/Desktop/PLANORA/
Sync         = app/src/ à jour avec VPS
Repo git     = clean-main up-to-date avec origin/main
```

### Structure finale du code (53 fichiers)
```
app/src/
├── App.jsx                          605 l (orchestrateur pur — routing + state global)
├── theme.js                         T_LIGHT, T_DARK, T, setTheme
├── main.jsx, App.css, index.css
│
├── data/
│   └── fixtures.js                  COMPANIES (bug c1), INIT_* mocks
│
├── shared/
│   ├── ui/                          21 atomics (Btn, Card, Modal, I, HookIsolator, …)
│   │   └── index.js                 (barrel)
│   ├── utils/
│   │   ├── phone.js                 formatPhoneFR, displayPhone
│   │   ├── validators.js            isValidEmail, isValidPhone
│   │   ├── constants.js             COMMON_TIMEZONES, genCode
│   │   ├── dates.js                 DAYS_FR, MONTHS_FR, fmtDate, getDow
│   │   ├── pipeline.js              PIPELINE_CARD_COLORS_DEFAULT, RDV_CATEGORIES
│   │   └── notifications.js         sendNotification, buildNotifyPayload
│   ├── services/
│   │   └── api.js                   api, recUrl, API_BASE, collectEnv
│   └── state/
│       └── tabState.js              _T (mutable tab-scoped, volatile)
│
└── features/
    ├── collab/                      ★ HUB COLLABORATEUR
    │   ├── CollabPortal.jsx         17 535 l (wrappé dans CollabProvider)
    │   ├── context/
    │   │   └── CollabContext.jsx    Provider + useCollabContext hook
    │   ├── tabs/                    ★ Tabs extraits via context (Phase 11+12+13+14)
    │   │   ├── AiProfileTab.jsx     ~300 l (Phase 11c)
    │   │   ├── TablesTab.jsx        ~280 l (Phase 11b)
    │   │   ├── MessagesTab.jsx      ~370 l (Phase 11a)
    │   │   ├── AvailabilityTab.jsx  ~280 l (Phase 12a)
    │   │   ├── ObjectifsTab.jsx     ~165 l (Phase 12b)
    │   │   ├── HomeTab.jsx          ~862 l (Phase 13a)
    │   │   ├── AgendaTab.jsx        ~857 l (Phase 13b)
    │   │   ├── CrmTab.jsx           ~1 732 l (Phase 14a)
    │   │   └── PhoneTab.jsx         ~8 766 l (Phase 14b — THE MASTODON)
    │   └── screens/                 (6 screens Phase 4)
    │
    ├── admin/                       ★ HUB ADMIN
    │   ├── AdminDash.jsx            11 775 l
    │   ├── data/
    │   │   └── templates.js         DEFAULT_TEMPLATES + TEMPLATE_VARS + applyTemplatePreview
    │   ├── forms/                   ★ Forms extraits (Phase 9a)
    │   │   ├── NewCollabForm.jsx
    │   │   ├── NewCompanyForm.jsx
    │   │   ├── NewCalForm.jsx
    │   │   ├── PlacesAutocomplete.jsx
    │   │   ├── TemplateEditorPopup.jsx
    │   │   └── index.js             (barrel)
    │   └── screens/                 (9 screens Phase 4)
    │
    ├── public/                      (Phase 6)
    │   ├── Landing.jsx
    │   ├── PublicBooking.jsx
    │   ├── PublicPage.jsx
    │   ├── ManageBooking.jsx
    │   ├── VisitorBooking.jsx
    │   └── PublicForm.jsx
    │
    └── client/
        └── ClientPortal.jsx
```

---

## 2. HISTORIQUE DES PHASES

Chronologique avec les commits git :

| Phase | Commit | App.jsx | CollabPortal | AdminDash | Contenu |
|---|---|---|---|---|---|
| 0 | `05c25e55` | 39 338 | (inline) | (inline) | HookIsolator sur sms-monitoring |
| 1A | `e2da334a` | 39 293 | — | — | theme + utils purs |
| 1B | `37623be9` | 38 984 | — | — | 21 UI atomics → `shared/ui/` |
| 2 | `c0a04efc` | 38 842 | — | — | fixtures + dates + pipeline + notifications |
| 3 | `46b7069a` | 38 815 | — | — | api service → `shared/services/api.js` |
| 4 | `f82ca9cc` | 34 655 | — | — | 15 HookIsolator screens (collab + admin) |
| 5 | `c60d537d` | 34 657 | — | — | Restructure `shared/` + `features/` |
| 5.5 | (dans 6) | 34 624 | — | — | `_T` → `shared/state/tabState.js` |
| 6 | `0a2321c4` | 31 916 | — | — | 7 composants publics/client extraits |
| Sync | `487a21fd` | 31 916 | — | — | Sync local app/src/ avec VPS |
| Doc | `579aedbc` | 31 916 | — | — | HANDOFF document |
| **7+8** | `4661301d` | **605** | 18 792 | 12 598 | **Move CollabPortal + AdminDash → fichiers dédiés** |
| **9a** | `759fdd2e` | 605 | 18 792 | **11 775** | **Split AdminDash forms → 6 fichiers** |
| **10+11** | `7d62bbf6` | 605 | **17 892** | 11 775 | **CollabContext + 3 IIFE tabs (AI Profile, Tables, Messages)** |
| **12a** | `03f7e9f8` | 605 | **17 653** | 11 775 | **AvailabilityTab extraction (−247 l)** |
| **12b** | `c248497a` | 605 | **17 535** | 11 775 | **ObjectifsTab extraction (−118 l)** |
| **13a** | `20c599b7` | 605 | **16 717** | 11 775 | **HomeTab extraction (−834 l)** ⚠ pas encore en prod (SSH timeout) |
| **13b** | `114b7831` | 605 | **15 923** | 11 775 | **AgendaTab extraction (−815 l)** |
| **14a** | `9702bf31` | 605 | **14 341** | 11 775 | **CrmTab extraction (−1 639 l)** |
| **14b** | `b2cc8379` | 605 | **5 832** | 11 775 | **PhoneTab extraction THE MASTODON (−8 636 l)** |

**Réduction totale App.jsx** : 39 338 → 605 lignes (**−98.5 %**)
**Réduction CollabPortal** : 18 792 → 5 832 lignes (**−69 %** depuis Phase 7) — 9 tabs extraits sur 12+

### Scripts par phase dans [v7-deploy/](v7-deploy/)

- `v7-deploy/phase7/move-collab-portal.js` — extrait CollabPortal de App.jsx
- `v7-deploy/phase7/move-admin-dash.js` — extrait AdminDash + forms
- `v7-deploy/phase7/split-admin-forms.js` — sépare les 6 forms admin
- `v7-deploy/phase7/replace-ai-profile-iife.js` — remplace l'IIFE par `<AiProfileTab/>`
- `v7-deploy/phase7/replace-tables-iife.js` — remplace l'IIFE par `<TablesTab/>`
- `v7-deploy/phase7/replace-messages-iife.js` — remplace l'IIFE par `<MessagesTab/>`

Lancer une phase manuellement : `node v7-deploy/phase7/<script>.js`
Build + deploy : voir §5 commandes utiles.

---

## 3. CE QUI RESTE À FAIRE

### Composition actuelle de CollabPortal.jsx (17 892 l)

Après Phase 11+12+13, les tabs déjà extraits :
- ✅ Messages (357 l → MessagesTab.jsx)
- ✅ Tables (270 l → TablesTab.jsx)
- ✅ AI Profile (344 l → AiProfileTab.jsx)
- ✅ Availability (248 l → AvailabilityTab.jsx, Phase 12a)
- ✅ Objectifs (119 l → ObjectifsTab.jsx, Phase 12b)
- ✅ **Home** (835 l → HomeTab.jsx, Phase 13a)
- ✅ **Agenda** (816 l → AgendaTab.jsx, Phase 13b)

**Tabs encore inline (à extraire en sessions futures)** :

| Tab | Lignes réelles | Closure refs | Difficulté | Stratégie |
|---|---|---|---|---|
| Settings | 829 | ~30+ | Élevé | Découper en 8 sub-sections sous tabs/settings/ |
| sms-monitoring | ~50 | ~5 | Bas | IIFE avec hooks (bug 16 latent), à wrapper HookIsolator |
| **Settings** | **829 réelles** | ~30+ | **Élevé** | ⚠ Hétérogène : profil + Google + AutoReply + SMS auto + Suggestions live + SMS pipeline + Power Dialer + AMD. **Découper en 8 sub-section files** avant d'extraire (`tabs/settings/ProfilSection.jsx`, `GoogleSection.jsx`, etc.). Bug latent `setCollab` non déclaré ligne 15755. |
| **CRM** | ~1 642 | ~100 | **Élevé** | Découper d'abord en sous-composants (fiche, table, pipeline view) |
| **Phone/VoIP/Pipeline Live** | ~8 639 | ~150 | **TRÈS ÉLEVÉ** | Le plus gros morceau — découper d'abord en : pipeline, history, conversations, contacts, call-detail, dialer |

### Composition actuelle de AdminDash.jsx (11 775 l)

Toujours monolithique. Les tabs admin sont inline dans le body de AdminDash. Pour les extraire :
1. Créer `features/admin/context/AdminContext.jsx` (même pattern que CollabContext)
2. Wrapper le return JSX de AdminDash dans `<AdminProvider>`
3. Extraire chaque tab admin un par un dans `features/admin/tabs/`

**Tabs admin identifiés** (audit Phase 7 antérieur) :
- Home admin, CRM admin, Vision (Faucon, Inscriptions), Forms admin
- Tables, Phone marketplace, VoIP config, Settings, Backup, Tickets

### Plan recommandé pour la prochaine session

**Phase 12c — Settings (à découper)** — DOIT être fait avant extraction
1. Settings tab fait 829 lignes hétérogènes — **PAS extractible en une fois safely**
2. Plan : créer `features/collab/tabs/settings/` avec un fichier par sub-section :
   - `ProfilSection.jsx` (sous-tab profil + notifs)
   - `GoogleSection.jsx` (intégration Google Calendar)
   - `AutoReplySection.jsx` (réponse automatique)
   - `SmsAutoSection.jsx` (SMS automatiques NRP/manqués)
   - `SuggestionsLiveSection.jsx` (déclencheurs en appel)
   - `SmsPipelineSection.jsx` (automatisation par colonne)
   - `PowerDialerSection.jsx` (ring timeout, AMD)
3. SettingsTab.jsx devient un router de sub-tabs minimal
4. Bonus : corriger le bug `setCollab` non déclaré ligne 15755 (timezone update)
5. Gain estim. : −800 lignes dans CollabPortal

**Phase 13** — Home + Agenda (séparément)
1. Phase 13a — HomeTab.jsx (~835 l)
2. Phase 13b — AgendaTab.jsx (~818 l) avec ses ~50 closure refs (weekOffset, viewMode, getBookingAt, etc.)
3. Gain estim. : −1 600 lignes

**Phase 14** — CRM (le plus chirurgical)
1. Sous-découper le tab CRM en : header/filters, table view, pipeline view, fiche contact, modals
2. Chaque sous-composant en fichier séparé sous `features/collab/tabs/crm/`
3. Gain estim. : −1 600 lignes

**Phase 15** — Phone/Pipeline Live (le mastodonte VoIP)
1. Découpage interne sous `features/collab/tabs/phone/` :
   DialerWidget, CallDetailPanel, PipelineKanban, ConversationView, CallHistory, ContactList
2. Plus complexe car beaucoup de hooks Twilio + temps réel + state machine VoIP
3. Gain estim. : −5 000 lignes (objectif final : CollabPortal < 5 000 l)

**Phase 16** — AdminContext + extractions admin
1. Créer `features/admin/context/AdminContext.jsx` sur le même pattern que CollabContext
2. Extraire tabs admin un par un sous `features/admin/tabs/`
3. Gain estim. : −8 000 lignes (objectif final : AdminDash < 4 000 l)

---

## 4. RÈGLES CRITIQUES (à respecter absolument)

### Architecture

1. **Structure cible** :
   - `shared/ui/` = composants UI atomiques (réutilisables partout)
   - `shared/utils/` = fonctions pures sans state
   - `shared/services/` = wrappers API
   - `shared/state/` = état global volatile (tabState)
   - `features/<domaine>/` = code métier par domaine
   - `features/<domaine>/screens/` = écrans métier (HookIsolator screens)
   - `features/<domaine>/tabs/` = tabs extraits via context
   - `features/<domaine>/context/` = contexts par domaine
   - `features/<domaine>/forms/` = formulaires métier
   - `features/<domaine>/data/` = constantes/templates métier

2. **Ne PAS remettre de code dans App.jsx** ❗ App.jsx est un **orchestrateur pur** (605 l). MH a explicitement demandé de **toujours** structurer le nouveau code dans la bonne hiérarchie. Si on ne sait pas où mettre un nouveau composant, demander avant.

3. **Les tabs extraits consomment le context** via `useCollabContext()` — pas de prop drilling.

4. **Pour étendre un tab existant** : modifier le fichier dédié (`features/collab/tabs/MessagesTab.jsx` etc.), pas dans CollabPortal.

5. **Pour ajouter un nouveau tab collab** :
   1. Créer `features/collab/tabs/NewTab.jsx` qui consomme le context
   2. Étendre `CollabContext` value dans CollabPortal si nouveaux refs nécessaires
   3. Ajouter `import NewTab from "./tabs/NewTab"` dans CollabPortal
   4. Ajouter `{portalTab === "new-tab" && <NewTab/>}` dans le JSX render

### Règles de refactor

6. **Une extraction = un patch = un commit = un deploy** (jamais chaîner plusieurs phases en un push)
7. **Backup auto avant modification** — utiliser `App.jsx.pre-<phase>-<ts>` ou équivalent
8. **Rollback prêt avant chaque deploy** — chaque commit est revertable individuellement
9. **Si extraction nécessite > 10 props → stop**, étendre le context d'abord
10. **Si un composant a > 1 500 lignes après extraction → subdiviser**

### Règles de build/deploy

11. **Jamais `--minify=false`** sur le VPS (OOM 3+GB RAM)
12. **Toujours copier dist → httpdocs** après `npm run build`
13. **Vérifier HTTP 200 + bundle hash** après deploy

### Backend (§0 de CLAUDE.md — règle absolue)

14. **Jamais `import { db } from '../db/database.js'` direct** dans les routes backend. Toujours via `tenantResolver`.
15. **Chaque nouvelle entreprise = sa propre DB SQLite** dans `/var/www/planora-data/tenants/`

---

## 5. COMMANDES UTILES

### SSH + état prod

```bash
# Connexion VPS
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115

# État prod rapide
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 "
  curl -s -o /dev/null -w 'HTTP %{http_code}\n' https://calendar360.fr/
  wc -l /var/www/planora/app/src/App.jsx /var/www/planora/app/src/features/collab/CollabPortal.jsx /var/www/planora/app/src/features/admin/AdminDash.jsx
  grep -oE 'index-[A-Za-z0-9_]+\.js' /var/www/vhosts/calendar360.fr/httpdocs/index.html
  pm2 list | grep calendar360
"

# Voir les logs d'erreur backend récents
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 "pm2 logs calendar360 --lines 30 --nostream --err | tail -20"
```

### Sync local → VPS

```bash
# Push code modifié au VPS
rsync -avz --exclude='*.bak*' --exclude='*.pre-*' --exclude='node_modules' \
  -e "ssh -i ~/.ssh/id_ed25519" \
  /Users/design/Desktop/PLANORA/app/src/ \
  root@136.144.204.115:/var/www/planora/app/src/
```

### Build local (test rapide sans toucher prod)

```bash
cd /Users/design/Desktop/PLANORA/app
./node_modules/.bin/vite build
# Sortie : dist/assets/index-XXXX.js + dist/index.html
```

### Build + deploy VPS — Smart Deploy (pipeline complet)

Depuis 2026-04-20, deploy via pipeline intelligent **TEST → FIX → DEPLOY → SMOKE → GIT → PUSH → MERGE** avec rollback automatique conditionnel.

```bash
# Pipeline complet (default — fait les 7 étapes)
./v7-deploy/deploy.sh "Phase 15 — extract Settings"   # avec commit msg custom
./v7-deploy/deploy.sh                                 # commit msg auto-généré

# Variantes
./v7-deploy/deploy.sh --check     # SMOKE TEST seul (vérifier prod actuelle)
./v7-deploy/deploy.sh --no-git    # Deploy sans commit/push/merge (juste sync+build+deploy+smoke)
./v7-deploy/deploy.sh --no-test   # Skip TEST local (déconseillé)

# Voir l'historique des incidents
./v7-deploy/incidents.sh           # 10 derniers deploys + verdict
./v7-deploy/incidents.sh latest    # détails du dernier deploy
./v7-deploy/incidents.sh critical  # liste des bugs CRITIQUES historiques
./v7-deploy/incidents.sh major     # liste des bugs MAJEURS historiques
./v7-deploy/incidents.sh d-XXX     # détails d'un deploy spécifique
```

**Pipeline étape par étape** :

| # | Étape | Action | Si échec |
|---|---|---|---|
| 0 | **PRECHECKS** | SSH OK, VPS health snapshot | abort |
| 1 | **TEST** | `vite build` LOCAL (catche compile errors avant VPS) | abort + message clair |
| 2 | **FIX** | (manuelle — l'utilisateur corrige et relance) | — |
| 3 | **DEPLOY** | backup + rsync + build VPS + cp dist→httpdocs | abort |
| 4 | **SMOKE** | 8 checks classifiés CRITIQUE/MAJEUR/MINEUR | si CRITIQUE → rollback auto |
| 5 | **GIT** | `git add` (app/src/, v7-deploy/, HANDOFF) + `git commit` | abort si CRITIQUE |
| 6 | **PUSH** | `git push origin clean-main:main` | abort + warning |
| 7 | **MERGE** | `git fetch + merge --ff-only origin/main` (sync remote) | warning si divergence |

**Logique smoke test (8 checks) :**

| Check | Bug si | Severity |
|---|---|---|
| `http-status` | HTTPS != 200 | CRITIQUE |
| `bundle-hash` | hash dans HTML ≠ hash buildé | CRITIQUE |
| `bundle-download` | bundle .js non téléchargeable ou taille hors fourchette 0.5-10 MB | CRITIQUE |
| `html-root` | `<div id="root">` manquant dans HTML | CRITIQUE |
| `bundle-content` | bundle ne contient pas symboles app | MAJEUR |
| `pm2-status` | calendar360 stopped/errored | CRITIQUE — sinon MAJEUR |
| `api-auth-me` | HTTP 5xx | CRITIQUE — sinon MAJEUR si autre que 200/401 |
| `backend-errors` | >30 errors backend depuis deploy | MAJEUR — sinon MINEUR si 5-30 |

**Verdict & action :**
- 1+ check CRITIQUE → **rollback automatique** (restore tarball httpdocs pré-deploy) + exit 2
- 1+ check MAJEUR → deploy maintenu, alerte "à corriger rapidement mais pas bloquant" + exit 1
- 1+ check MINEUR → deploy OK, "à corriger plus tard" + exit 0
- 0 check failed → SUCCESS + exit 0

**Log persistant :** `/var/log/planora-deploys.jsonl` sur VPS (1 ligne JSON par check, 1 ligne par rollback).

### Deploy manuel legacy (sans smoke test)

```bash
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 "
  cd /var/www/planora/app && npm run build
  TS=\$(date +%Y%m%d-%H%M%S)
  tar czf /var/backups/planora/httpdocs/httpdocs-pre-deploy-\${TS}.tar.gz -C /var/www/vhosts/calendar360.fr httpdocs/
  rm -rf /var/www/vhosts/calendar360.fr/httpdocs/assets
  cp -r dist/* /var/www/vhosts/calendar360.fr/httpdocs/
  curl -s -o /dev/null -w 'HTTP %{http_code}\n' https://calendar360.fr/
  grep -oE 'index-[A-Za-z0-9_]+\.js' /var/www/vhosts/calendar360.fr/httpdocs/index.html
"
```

### Git

```bash
# Push vers origin/main
git push origin clean-main:main

# Voir tous les commits du refactor
git log --oneline 7fe0e80c..HEAD

# Rollback ciblé d'un commit (par ex. annuler Phase 11 si bug remonte en prod)
git revert 7d62bbf6
```

---

## 6. CONTRAINTES ET PIÈGES

### SSH depuis le sandbox Claude

**Fonctionne** (testé et validé pendant les 4 sessions). L'ancienne note dans CLAUDE.md §6 disait bloqué — c'est obsolète.

### Build local : restaurer package.json/vite.config.js si manquants

Le projet local n'a pas toujours ces fichiers ; ils ont été restaurés depuis `backups/V7-backup-20260417/app/` pour permettre le build local. S'ils manquent à nouveau :
```bash
cp backups/V7-backup-20260417/app/package.json app/
cp backups/V7-backup-20260417/app/vite.config.js app/
cp backups/V7-backup-20260417/app/index.html app/
```

### Bug connu, non résolu, NON prioritaire

**Company ID `c1` en prod** : le frontend envoie `companyId=c1` (valeur de fixture/mock hardcodée à [data/fixtures.js](app/src/data/fixtures.js) ligne 3) au lieu du vrai ID. Cause les erreurs `400 /api/messaging?companyId=c1`. À traiter dans une PR dédiée.

### Pattern documenté : bugs de classe 16

IIFE `{(()=>{...})()}` contenant `useState`/`useEffect` dans un tab conditionnel = React #310 dès qu'on change de tab. Solution : wrap dans `<HookIsolator>`. Quand on extrait un IIFE en composant via context, ce risque disparaît (le composant a son propre fiber).

### Backend errors pré-existants (NON liés au refactor)

Dans `pm2 logs calendar360 --err` :
- `[VOIP SECURITY] Twilio signature mismatch` — warning non-bloquant
- `[V7] transfer error: ...` — fixés le matin du 2026-04-19, logs antérieurs

---

## 7. BACKUPS / ROLLBACK

### Triple redondance disponible

| Emplacement | Contenu | Utilité |
|---|---|---|
| GitHub `origin/main` | 14 commits du refactor | Source de vérité |
| Mac `~/Desktop/PLANORA/backups/` | Tarballs avec SHA256 | Rollback hors-git |
| VPS `/var/backups/planora/refactor/` | Snapshots App.jsx pré-phase | Rollback in-place |
| VPS `/var/backups/planora/httpdocs/` | Tarballs httpdocs pré-deploy | Rollback bundle servi |

### Rollback granulaire

Chaque phase est un commit atomique. Pour annuler Phase 11 sans toucher Phase 9a/7+8 :
```bash
git revert 7d62bbf6  # annule Phase 10+11
git push origin clean-main:main
# puis rsync + build + deploy
```

### Rollback complet (retour App.jsx 31 916 lignes)

```bash
git reset --hard 579aedbc  # avant Phase 7
git push --force-with-lease origin clean-main:main
# ⚠ destructif — confirmer avec MH avant
```

---

## 8. POUR REPRENDRE LA SESSION

### Si tu veux continuer Phase 12 (extraction tabs faciles : Objectifs + Availability + Settings)

1. Lire [CLAUDE.md](CLAUDE.md) §0 (règle isolation DB)
2. Lire ce handoff en entier
3. **Mémoriser** : ne JAMAIS rajouter de code dans App.jsx — toujours dans la bonne hiérarchie
4. Modèle à reproduire : `v7-deploy/phase7/replace-ai-profile-iife.js`
5. Pour chaque tab à extraire :
   - Identifier les closure refs avec un agent Explore
   - Étendre `CollabContext` value dans CollabPortal
   - Créer `features/collab/tabs/<NewTab>.jsx` qui consomme le context
   - Remplacer l'IIFE par `<NewTab/>` (script Node similaire aux 3 existants)
   - Build local → sync VPS → build VPS → deploy → smoke test
6. Commit + push après chaque tab extrait avec succès

### Si tu veux auditer / vérifier l'état avant de continuer

```bash
cd /Users/design/Desktop/PLANORA
git log --oneline -5
git status

ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 "
  curl -s -o /dev/null -w 'HTTP %{http_code}\n' https://calendar360.fr/
  wc -l /var/www/planora/app/src/App.jsx /var/www/planora/app/src/features/collab/CollabPortal.jsx /var/www/planora/app/src/features/admin/AdminDash.jsx
"
```

### Fichiers clés à ouvrir en premier

- [app/src/App.jsx](app/src/App.jsx) — l'orchestrateur (605 l)
- [app/src/features/collab/CollabPortal.jsx](app/src/features/collab/CollabPortal.jsx) — le hub collab refactoré (17 892 l)
- [app/src/features/collab/context/CollabContext.jsx](app/src/features/collab/context/CollabContext.jsx) — Provider + hook
- [app/src/features/collab/tabs/AiProfileTab.jsx](app/src/features/collab/tabs/AiProfileTab.jsx) — modèle de tab extrait
- [app/src/features/admin/AdminDash.jsx](app/src/features/admin/AdminDash.jsx) — hub admin (11 775 l)
- [app/src/features/admin/forms/index.js](app/src/features/admin/forms/index.js) — barrel forms admin
- [v7-deploy/phase7/replace-ai-profile-iife.js](v7-deploy/phase7/replace-ai-profile-iife.js) — template de script de move

---

## 9. EN COURS — AUDIT DB monolithe vs tenants (2026-04-20)

> Tâche démarrée à la fin de la session 2026-04-20.
> **Audit terminé. Aucune correction effectuée. 6 décisions à prendre par MH avant la Phase A.**
>
> Rapport complet : [AUDIT-DB-2026-04-20.md](AUDIT-DB-2026-04-20.md)

### Résumé express

| DB | Tables | Schema | Données |
|---|---|---|---|
| monolithe (`calendar360.db`) | 95 | source de vérité structurelle | 13 doublons emails, 9 doublons phones, 32 bookings + 48 call_logs orphelins |
| CapFinances (`c1776169036725.db`) | 89 | manque 20 cols + 6 idx + 2 triggers | ✅ propre (0 orphelin) |
| MonBilan (`c-monbilan.db`) | 89 | idem CapFinances | 30 bookings sans contact (probablement RDV publics) |

⚠ **`PRAGMA foreign_keys = 0` sur les 3 DBs** — déclarations FK décoratives, jamais appliquées.

### Diff structurel à propager du monolithe vers les 2 tenants

- `bookings` : 8 cols manquantes (V7 transfer : agendaOwner, meetingCollab, bookingType, bookingOutcome…) + 3 indexes
- `contacts` : 8 cols (V7 owner/executor/visibilityScope/lastMeeting*) + 3 indexes
- `collaborators` : 4 cols (acceptInternalMeetings, autoAcceptMeetings, meetingPriorityLevel, shareAgendaAvailability)
- `audit_logs` : 2 triggers (immutabilité)

Total : **20 cols + 6 indexes + 2 triggers** — toutes additives non destructives (`ALTER TABLE ADD COLUMN` avec DEFAULT).

### 6 décisions en attente côté MH

1. **Schéma cible** : confirmer monolithe = source de vérité structurelle (= propager les 20 cols + 6 idx + 2 triggers vers tenants) ?
2. **Sessions** (table 75 rows) : laisser dans monolithe seul (recommandé) ou migrer dans tenants ?
3. **30 bookings MonBilan sans contact** : laisser tel quel (RDV publics by-design) ou nettoyer ?
4. **13 doublons emails monolithe** : décision case-by-case (Claude génère la liste) ou cleanup auto ?
5. **48 call_logs monolithe orphelins** : delete (perdre historique) ou laisser (rejet par futur FK) ?
6. **Activation FKs** : after nettoyage, on active `PRAGMA foreign_keys = ON` partout ou on reste en statu quo ?

### Plan de correction (à exécuter une fois validation reçue)

| Phase | Action | Risque |
|---|---|---|
| **A** | Propager schéma monolithe → tenants (20 cols + 6 idx + 2 triggers) | **0** (additif, idempotent) |
| **B** | Investiguer + nettoyer 30 bookings MonBilan | Faible |
| **C** | Audit + cleanup case-by-case des doublons + orphelins monolithe | Modéré |
| **D** | Activer `foreign_keys = ON` dans `database.js` + `tenantResolver.js` | Élevé (peut révéler de nouveaux orphelins) |
| **E** | Re-audit complet + smoke test prod | 0 |

Backup auto VPS avant chaque phase. Modifications en transaction. `PRAGMA integrity_check` après chaque modif.

---

## 10. CONTACT

- **Mainteneur principal** : MH — rc.sitbon@gmail.com (supra admin)
- **Repo GitHub** : https://github.com/romain613/Planora
- **Prod** : https://calendar360.fr/
- **VPS** : 136.144.204.115 (TransIP, Debian 12 + Plesk)
- **Règles projet** : [CLAUDE.md](CLAUDE.md)
- **Règles frontend refactor** : ce document (HANDOFF-2026-04-19.md)

---

## 11. HANDOFF SESSION 2026-04-20 — REPRISE NOUVELLE DISCUSSION

> Document de transition pour qu'une nouvelle instance Claude reprenne sans perdre le contexte.
> **Lire en priorité** : ce §11 + [CLAUDE.md](CLAUDE.md) + les rapports cités ci-dessous.

### 11.1 Phases exécutées et validées dans la session 2026-04-20

| Phase | Statut | Rapport | Audit JSON | Script |
|---|---|---|---|---|
| **A** propagation schéma → tenants | ✅ DONE | [PHASE-A-REPORT-2026-04-20.md](PHASE-A-REPORT-2026-04-20.md) | — | [db-migrations/2026-04-20-phaseA-propagate-schema.js](db-migrations/2026-04-20-phaseA-propagate-schema.js) |
| **B** audit MonBilan bookings sans contact | ✅ DONE | [PHASE-B-REPORT-2026-04-20.md](PHASE-B-REPORT-2026-04-20.md) | — | — |
| **B-execute** stratégie E sur MonBilan tenant | ✅ DONE | [PHASE-B-EXECUTE-REPORT-2026-04-20.md](PHASE-B-EXECUTE-REPORT-2026-04-20.md) | [db-migrations/2026-04-20-phaseB-audit-output.json](db-migrations/2026-04-20-phaseB-audit-output.json) | [db-migrations/2026-04-20-phaseB-execute-monbilan-orphans.js](db-migrations/2026-04-20-phaseB-execute-monbilan-orphans.js) |
| **C** audit dette monolithe | ✅ DONE | [PHASE-C-REPORT-2026-04-20.md](PHASE-C-REPORT-2026-04-20.md) | — | — |
| **C-1** bookings orphans monolithe | ✅ DONE | [PHASE-C1-EXECUTE-REPORT-2026-04-20.md](PHASE-C1-EXECUTE-REPORT-2026-04-20.md) | [db-migrations/2026-04-20-phaseC1-audit-output.json](db-migrations/2026-04-20-phaseC1-audit-output.json) | [db-migrations/2026-04-20-phaseC1-execute-monolithe-bookings.js](db-migrations/2026-04-20-phaseC1-execute-monolithe-bookings.js) |
| **C-2** call_logs orphans monolithe | ✅ DONE | [PHASE-C2-EXECUTE-REPORT-2026-04-20.md](PHASE-C2-EXECUTE-REPORT-2026-04-20.md) | [db-migrations/2026-04-20-phaseC2-audit-output.json](db-migrations/2026-04-20-phaseC2-audit-output.json) | [db-migrations/2026-04-20-phaseC2-execute-monolithe-call-logs.js](db-migrations/2026-04-20-phaseC2-execute-monolithe-call-logs.js) |
| **C-3** dé-assignation Préau (efef gardé) | ✅ DONE | [PHASE-C3-EXECUTE-REPORT-2026-04-20.md](PHASE-C3-EXECUTE-REPORT-2026-04-20.md) | [db-migrations/2026-04-20-phaseC3-audit-output.json](db-migrations/2026-04-20-phaseC3-audit-output.json) | [db-migrations/2026-04-20-phaseC3-execute-monolithe-deassign-preau.js](db-migrations/2026-04-20-phaseC3-execute-monolithe-deassign-preau.js) |
| **C-4** fix frontend `c1` + DELETE 40 contacts | ✅ DONE | [PHASE-C4-EXECUTE-REPORT-2026-04-20.md](PHASE-C4-EXECUTE-REPORT-2026-04-20.md) | [db-migrations/2026-04-20-phaseC4-audit-output.json](db-migrations/2026-04-20-phaseC4-audit-output.json) | [db-migrations/2026-04-20-phaseC4-execute-monolithe-cleanup-c1-contacts.js](db-migrations/2026-04-20-phaseC4-execute-monolithe-cleanup-c1-contacts.js) |
| **D-0** audit FK pre-flight read-only | ✅ DONE | [PHASE-D-PRECHECK-REPORT.md](PHASE-D-PRECHECK-REPORT.md) | [db-migrations/2026-04-20-phaseD-precheck.json](db-migrations/2026-04-20-phaseD-precheck.json) | [db-migrations/2026-04-20-phaseD-precheck-readonly.js](db-migrations/2026-04-20-phaseD-precheck-readonly.js) |
| **D-0bis** cleanup violations FK historiques | ✅ DONE | [PHASE-D0BIS-EXECUTE-REPORT.md](PHASE-D0BIS-EXECUTE-REPORT.md) | [db-migrations/2026-04-20-phaseD0bis-audit-output.json](db-migrations/2026-04-20-phaseD0bis-audit-output.json) | [db-migrations/2026-04-20-phaseD0bis-cleanup-historical-violations.js](db-migrations/2026-04-20-phaseD0bis-cleanup-historical-violations.js) |
| **D-1bis** tests locaux | ⏸ SKIP confirmé MH | — | — | — |
| **D-2** activation FK ON prod | ❌ OBSOLÈTE (déjà actif depuis longtemps via code backend) | — | — | — |
| **D-3 Step 2** monitor live (script + service systemd) | ✅ DONE | (récap dans cette §11) | — | [ops/d3/planora-fk-monitor.sh](ops/d3/planora-fk-monitor.sh) + [ops/d3/planora-fk-monitor.service](ops/d3/planora-fk-monitor.service) |
| **D-3 Step 3-9** | ⏸ EN ATTENTE | — | — | — |

### 11.2 État DB final mesuré (post Phase D-0bis)

| Métrique | Pré-session | Post-session 2026-04-20 |
|---|---|---|
| Bookings orphans monolithe | 32 | **0** ✅ |
| Call_logs orphans monolithe | 48 | **0** ✅ |
| Contacts c1 polluants | 40 | **0** ✅ |
| Contacts→collab orphans | 2 | 1 (efef volontairement gardé) |
| Doublons emails groupes | 13 | 3 (cross-company légitimes MH) |
| Doublons phones groupes | 9 | 3 (cross-company légitimes MH) |
| google_events orphans | 117 | 0 |
| roles orphans monolithe | 12 | 0 (+ cascade auto sur 84 role_permissions) |
| tickets bug orphans monolithe | 2 | 0 (+ 2 ticket_messages cascadés) |
| Placeholder `__deleted__` | mono uniquement | présent sur les 3 DBs |
| `PRAGMA foreign_keys` runtime | déjà ON (révélé en D-0) | confirmé ON |
| **`PRAGMA foreign_key_check` total** | **131** | **0** ✅✅✅ |
| Schéma monolithe ↔ tenants | divergent (4 tables) | aligné (post Phase A) |
| `integrity_check` (3 DBs) | ok | ok |
| Prod HTTPS | 200 | 200 |
| pm2 calendar360 | online | online |

### 11.3 État D-3 Step 2 (monitor FK installé sur VPS)

| Composant | Chemin / valeur |
|---|---|
| Service systemd | `planora-fk-monitor.service` (`enabled` + `active running`) |
| MainPID | 48686 (depuis 2026-04-20T08:18:57Z) |
| NRestarts | 0 |
| Script monitor | `/usr/local/bin/planora-fk-monitor.sh` (tail pm2 logs → grep FK → append log) |
| Log dédié | `/var/log/planora-fk-violations.log` (mode 640 root:adm, 0 violation FK observée à ce jour) |
| Sources locales versionnées | [ops/d3/planora-fk-monitor.sh](ops/d3/planora-fk-monitor.sh) + [ops/d3/planora-fk-monitor.service](ops/d3/planora-fk-monitor.service) |
| Fixes appliqués pendant install | (1) `--nostream=false` retiré (flag boolean pm2 v6, pas =value), (2) `ProtectHome=true` retiré (bloquait `/root/.pm2/` socket god daemon), (3) `Environment=HOME=/root` + `WorkingDirectory=/root` ajoutés |
| Verdict Step 2 | ✅ GO pour Step 3 |

### 11.4 Backups triple-redondance

| Backup | SHA-256 | Localisations |
|---|---|---|
| Full backup post-C-4 | `f9e7be8e0bbf83ddd71651660ecba822928b0f2f55e3fc3400e3a96a5ec3b508` | VPS + Mac + iCloud (cf. [BACKUP-COMPLET-2026-04-20.md](BACKUP-COMPLET-2026-04-20.md)) |
| Backup pré-D-0bis | `a194e0ba9298fb7bfe80508c147e2c16f57c103eea29a3476baa6fba158bd95a` | VPS + Mac + iCloud |
| Backup pré-C-4 | `a0d28de31559bb10704c88c669d067abe048aea3ff302dfa2765a1faeed5f8de` | VPS |
| Backup pré-C-3 | `f03245cd1cbdd725844a683dd45bff77f7a03b07354870bb469ad7ecee829ced` | VPS |
| Backup pré-C-2 | `a004ace48b0690f98d554a469cb6c474027f42151e643e65bf5e3db9324f3027` | VPS |
| Backup pré-C-1 | `ae0dd9fb11f7f106ccad76626f897e488f5a904f3096cb12a8b624794aed6209` | VPS |
| Backup pré-B-execute | `20555f453424256966fffb35b5b8418cb4d826b6a86d133e13c4d4408aa14f21` | VPS |
| Backup pré-A | `3217be8835359946b5b3395cadbd385d7f651b4d5ce049a2143a5dae5c1a563e` | VPS |

### 11.5 Git

- Commit unique session : **`6ad2e34b`** (41 fichiers, +13497/−790)
- Pushé : `42bf7beb..6ad2e34b clean-main → main`
- Branch locale : `clean-main` synchronisée avec `origin/main`
- Repo : `https://github.com/romain613/Planora`
- Co-Author : `Claude Opus 4.7 (1M context) <noreply@anthropic.com>`

### 11.6 Découvertes architecturales notables (à garder en tête)

1. **`PRAGMA foreign_keys = ON` est DÉJÀ actif en prod** au runtime via `server/db/database.js:19`, `server/db/tenantDbCache.js:51`, `server/db/controlTower.js:29`. L'audit AUDIT-DB-2026-04-20 initial annonçait FK=0 mais c'était mesuré via CLI sqlite3 (dont défaut OFF) ≠ connexions backend Node.
2. **FK enfant non documentée** : `ticket_messages.ticketId → tickets.id ON DELETE NO ACTION`. **Tout DELETE FROM tickets DOIT supprimer les ticket_messages enfants d'abord**. Découverte D-0bis A3.
3. **FK CASCADE comportements voulus** :
   - `contact_followers.contactId → contacts.id` (V7)
   - `role_permissions.roleId → roles.id` (RBAC)
4. **Mode shadow** : c-monbilan + CapFinances en mode `shadow` = écritures vers monolithe + lecture parallèle tenant. Donc **les écritures runtime vont au monolithe**, pas aux tenants — explique pourquoi D-1bis (tester sur tenants) était inutile.
5. **Cas efef** : contact `ct1774872603359` toujours assigné à collab inexistant `u-rcsitbon`. **PAS un blocage FK** car la FK `contacts.assignedTo → collaborators.id` n'existe PAS dans le schéma. À traiter dans une future "phase nettoyage tests" si désiré.
6. **Cas c1 (Calendar360)** est une vraie company. Les 40 fixtures supprimées pointaient sur cette company à cause du bug `useState(COMPANIES[0])` (corrigé en C-4). Ne pas confondre la company c1 (à conserver) avec les 40 contacts pollution.

### 11.7 ✅ CE QUI RESTE À FAIRE (D-3 Steps 3-9)

Plan complet validé par MH (cf. discussion 2026-04-20). Ordre strict avec validation MH par sous-step :

| Step | Action | Validation MH requise avant |
|---|---|---|
| **3** | Install logrotate `/etc/logrotate.d/planora-fk-violations` | OUI |
| **4** | Vérification stabilité service 5 min | OUI |
| **5** | Install cron audit hebdo : dossier `/var/log/planora-fk-audits/` + script `/usr/local/bin/planora-fk-weekly-audit.sh` + cron `/etc/cron.d/planora-fk-weekly-audit` | OUI |
| **6** | Test manuel immédiat du wrapper d'audit (sans attendre lundi 3h UTC) | OUI |
| **7** | Édition `CLAUDE.md` ajout §0bis "FK runtime" | OUI |
| **8** | Édition `HANDOFF` ajout §10bis "FK monitoring" (cf. plan D-3.3) | OUI |
| **9** | Génération `PHASE-D3-INSTALL-REPORT.md` + transition vers observation 7 jours | OUI |

Contenu exact des fichiers Steps 3+5 et diffs Steps 7+8 : voir le **mini runbook D-3 finalisé dans la discussion 2026-04-20** (recopier dans nouvelle discussion ou demander à Claude de regénérer à partir de [PHASE-D-PRECHECK-REPORT.md](PHASE-D-PRECHECK-REPORT.md) + [PHASE-D0BIS-EXECUTE-REPORT.md](PHASE-D0BIS-EXECUTE-REPORT.md) + cette §11).

### 11.8 Après D-3 — clôture Phase D + sécurité data

Une fois D-3 Steps 3-9 terminés + 7 jours d'observation `0 SQLITE_CONSTRAINT_FOREIGNKEY` :
- Génération `PHASE-D-CLOSURE-REPORT.md`
- Édition finale CLAUDE.md §0bis + HANDOFF §10bis avec date de clôture

Ensuite chantier **Sécurité data** (à cadrer) :
- Soft delete (colonne `deleted_at` ou table miroir)
- Corbeille / restore (interface admin)
- Audit log enrichi (qui a deleted quoi, quand, pourquoi)

Et ultérieurement **Outils alignement futur** (point 7 brief initial MH) :
- `db-migrations/diff-schema.js` (comparaison auto monolithe vs tenants)
- Schema versioning (`_schema_meta` table)
- Pre-commit hook git pour bloquer toute migration non propagée

### 11.9 Règles strictes pour la nouvelle discussion

À rappeler au futur Claude au début de la nouvelle discussion :

- **NO BLIND CODING**, raisonner à partir de l'état réel
- Toujours **distinguer lecture / plan / exécution**
- Ne jamais exécuter sans validation MH explicite
- **Backup avant modification**, **transaction atomique**, **integrity_check avant/après**
- **Audit JSON** versionné dans `db-migrations/`
- **Rapport markdown** par phase
- Aucun git commit/push auto sans demande
- Aucun deploy sans autorisation explicite
- **Aucune action hors périmètre validé**

### 11.10 Comment reprendre dans la nouvelle discussion

Pour la nouvelle instance Claude :
1. **Lire CLAUDE.md** intégralement (règle §0 isolation DB + tout le reste)
2. **Lire ce HANDOFF §11** intégralement
3. **Lire** au minimum [PHASE-D0BIS-EXECUTE-REPORT.md](PHASE-D0BIS-EXECUTE-REPORT.md) + [PHASE-D-PRECHECK-REPORT.md](PHASE-D-PRECHECK-REPORT.md) pour comprendre l'état FK
4. **Demander à MH** par quel step continuer (typiquement D-3 Step 3 = logrotate)
5. **Respecter le mini runbook strict step-by-step** avec validation MH

---

## 12. HANDOFF SESSION 2026-04-20 (fin journée) — FRONTEND STABILISÉ + RÈGLE ARCHITECTURE GRAVÉE

> Document de transition après la stabilisation complète du frontend collab.
> Successeur de §11. À lire APRÈS §11 pour la chronologie complète.

### 12.1 Résumé de la session (sujet principal = frontend)

Bug initial : écran rouge `Cannot access 'Te' before initialization` au login collab en prod. Enquête a révélé une dette systémique Phase 14b (extraction PhoneTab de CollabPortal sans branchement complet via context).

**Boucle patch unitaire abandonnée** à la 6ᵉ vague (TDZ/undefined → fix → nouveau crash) au profit d'un **rewire global** en une passe :
- Audit exhaustif 9 tabs (useCollabContext consumers) : 123 orphelins identifiés
- Classification A (render-blockers) / B (handlers deferred) / C (destructifs par nom)
- Rewire groupé : 207 lignes ajoutées sur 6 fichiers source (value block CollabPortal + 5 destructures tab)
- Audit final post-rewire : **0 orphelin restant** sur les 9 tabs
- Fix AudioContext autoplay : Twilio Device init déféré au 1er user gesture (click/keydown/touchstart, `{once,capture,passive}`) dans CollabPortal + AdminDash
- Règle d'architecture gravée : [CLAUDE.md §0bis](CLAUDE.md) + [CollabContext.jsx](app/src/features/collab/context/CollabContext.jsx) en-tête

### 12.2 État prod courant

| Item | Valeur |
|---|---|
| Bundle prod actif | `index-Cndeq2z0.js` |
| SHA-256 (VPS = local build commité) | `c2ffdbb1a3e27b6a1c0e6f05619ef4f398d485f1fa12540dac903f853320b44c` |
| Git HEAD `main` + `clean-main` | `4dfac1a4` |
| HTTP GET `/` + bundle | 200 / 200 |
| Smoke V2 (login api-login + tour home/phone/crm/agenda) | critical=0, console=0, network=0 |
| Runtime console logs observés | `[COLLAB VOIP] Initializing device...` désormais déclenché **après** user gesture (plus de warning AudioContext) |

### 12.3 Commits de la session

| Hash | Rôle | Fichiers |
|---|---|---|
| `99eeec43` | Rewire complet + VoIP lazy init + règle §0bis | 10 source/doc (+431/-64) |
| `377beacc` | Untrack `app/dist/` build artifacts + gitignore | 3 (+1/-549) |
| `4dfac1a4` | Add `ops/smoke/` architecture health checker | 7 (+1134) |

Pushés sur `origin/clean-main` et fast-forward vers `origin/main`. Working tree **100% clean** à la fin.

### 12.4 Règle d'architecture frontend (IMPÉRATIVE à partir d'ici)

Gravée à 2 endroits de vérité croisés :
- [CLAUDE.md §0bis](CLAUDE.md#0bis-règle-fondamentale--architecture-frontend-branchement-des-symboles) — règle organisationnelle niveau projet, même priorité que §0 DB
- [app/src/features/collab/context/CollabContext.jsx](app/src/features/collab/context/CollabContext.jsx) — règle en tête du fichier provider

**Principe** :
- *Local reste local* — un symbole utilisé dans un seul scope ne doit pas être transporté via context
- *Partagé passe explicitement par le context* — 3 étapes obligatoires : déclarer top-level parent → exposer shorthand value → destructurer dans consommateur
- *Aucun symbole flottant, aucun ancien chemin conservé*

Pour toute nouvelle app / module / feature : créer le dossier propre d'emblée (`features/<domaine>/<feature>/{tabs,context,components,helpers}`), brancher immédiatement via context, auditer avant merge avec `ops/smoke/`.

### 12.5 Outil d'audit versionné — `ops/smoke/`

3 scripts Playwright headless, commit `4dfac1a4` :

| Script | Rôle | Usage |
|---|---|---|
| [`ops/smoke/collab-smoke.mjs`](ops/smoke/collab-smoke.mjs) | V1 — page login seule + bundle static check | `SMOKE_EMAIL=... SMOKE_PASS=... npm run smoke` |
| [`ops/smoke/collab-tour.mjs`](ops/smoke/collab-tour.mjs) | V2 — login + visite chaque tab via `localStorage.c360-portalTab` + reload | `npm run tour` |
| [`ops/smoke/collab-click.mjs`](ops/smoke/collab-click.mjs) | V3 — V2 + 1 safe click non-destructif par tab + Esc close, **fail-fast auth** | `npm run click` |

Installation 1 fois : `cd ops/smoke && npm install && npx playwright install chromium` (~92 Mo).
Sorties : `last-{report,tour-report,click-report}.json` (ignorés par git).
Exit code 1 si ≥1 pageerror ou ErrorBoundary visible.

### 12.6 État backend/data — où on s'est arrêté pour reprise

**Chantiers FERMÉS proprement** (session 2026-04-20 début — cf. §11) :
- Phase A, B, B-execute, C, C-1, C-2, C-3, C-4 : toutes données monolithe/tenant nettoyées
- Phase D-0 + D-0bis : `PRAGMA foreign_key_check = 0` sur les 3 DBs
- Phase D-3 Step 2 : monitor FK systemd actif — `planora-fk-monitor.service` enabled + running, 0 violation observée

**Chantiers OUVERTS à reprendre** :

1. **D-3 Steps 3-9** (infra FK monitoring) :
   - Step 3 — install logrotate `/etc/logrotate.d/planora-fk-violations`
   - Step 4 — vérif stabilité service 5 min
   - Step 5 — cron audit hebdo (dossier `/var/log/planora-fk-audits/` + script + cron)
   - Step 6 — test manuel wrapper
   - Step 7 — CLAUDE.md §0bis "FK runtime" (NOTE : §0bis est maintenant pris par la règle architecture frontend — renommer en §0ter ou autre)
   - Step 8 — HANDOFF §10bis "FK monitoring"
   - Step 9 — `PHASE-D3-INSTALL-REPORT.md` + 7 jours observation

2. **Phase E (nouveau chantier demandé par MH 2026-04-20 fin)** — audit bidirectionnel schéma mono↔tenants + source de vérité + alignement FK propres. 6 sous-étapes proposées (non exécutées, en attente GO MH) :
   - E.1 Cartographie 3 DBs (tables × colonnes × contraintes × indexes)
   - E.2 Data diff (orphans, divergences, doublons)
   - E.3 Définition source de vérité par table × company
   - E.4 Plan correction données
   - E.5 Plan alignement schéma
   - E.6 Plan ajout FK sans casser l'existant

3. **Projet 26** V7 étendu (transfert + RDV + suivi live + reporting) — spec reçue §8 §11, en attente de tranchage des 7 ambiguïtés par MH
4. **Sécurité data** (soft delete, corbeille, audit log) — non démarré
5. **Outils alignement futur** (schema-diff.js, schema_meta versioning, pre-commit hook) — non démarré

### 12.7 Backups & rollback — fin de session

Backups locaux tracés (sur Mac MH) :
- `*.pre-rewire-20260420.bak` (6 fichiers source post-vague1b, avant rewire groupé)
- `*.pre-vague1b-20260420.bak` (5 fichiers source pre-vague1b)
- Tous ignorés par `.gitignore` (pattern `*.pre-*.bak`)

Backups VPS (tracés via `index.html.pre-<phase>-20260420`) :
- `pre-fix-Te-20260420`, `pre-fix-companyId-20260420`, `pre-vague1-20260420`, `pre-fmtDur-20260420`, `pre-toggleLeft-20260420`, `pre-toggleDND-20260420`, `pre-vague1a-20260420`, `pre-vague1b-20260420`, `pre-audioctx-20260420`, `pre-rewire-20260420` — 10 snapshots successifs de `index.html`
- 8 bundles JS orphelins présents sur VPS comme fallbacks historiques

Rollback prod = restaurer un `index.html.pre-<X>` + rebuild éventuel depuis les sources du tag git correspondant.

### 12.8 Règles strictes à conserver pour la nouvelle session

À rappeler au futur Claude :

- Règle §0 CLAUDE.md : 1 client = 1 DB dédiée, passage systématique par `tenantResolver`
- Règle §0bis CLAUDE.md : branchement frontend via context, aucun symbole flottant
- NO BLIND CODING — raisonner à partir de l'état réel
- Toujours distinguer lecture / plan / exécution — validation MH explicite à chaque étape
- Backup avant modification, transaction atomique, integrity_check avant/après
- Audit JSON versionné dans `db-migrations/`, rapport markdown par phase
- Aucun commit / push / deploy auto sans demande
- Aucune action hors périmètre validé

### 12.9 Comment reprendre dans la nouvelle discussion

Pour la nouvelle instance Claude :
1. **Lire CLAUDE.md** intégralement (§0 DB isolation + §0bis architecture frontend + reste)
2. **Lire ce HANDOFF §12** intégralement (+ §11 pour chronologie backend Phase A-D)
3. **Demander à MH** par quel chantier continuer — candidats principaux :
   - **Phase E** : audit bidirectionnel schéma mono↔tenants (nouveau, demandé fin 2026-04-20)
   - **D-3 Steps 3-9** : finaliser infra FK monitoring
   - **Projet 26** : si MH a tranché les 7 ambiguïtés V7 étendu
4. **Respecter le mini runbook strict step-by-step** avec validation MH à chaque step
5. **Respecter la règle §0bis** : si la session touche au frontend ou à une nouvelle feature, créer le dossier propre d'emblée, brancher via context, auditer avec `ops/smoke/` avant merge

### 12.10 Commandes utiles pour la reprise

```bash
# État git
cd /Users/design/Desktop/PLANORA && git log --oneline -5 && git status

# Rebuild + redeploy si besoin (reproductible depuis le commit)
cd /Users/design/Desktop/PLANORA/app && npm run build
# puis scp index.html + dist/assets/*.js → VPS httpdocs/

# Smoke V3 architecture health (nécessite credentials collab)
cd /Users/design/Desktop/PLANORA/ops/smoke
SMOKE_EMAIL='...' SMOKE_PASS='...' npm run click

# État FK monitor sur VPS
ssh -i ~/.ssh/id_ed25519 root@136.144.204.115 \
  "systemctl status planora-fk-monitor.service --no-pager ; \
   wc -l /var/log/planora-fk-violations.log"

# Audit bidirectionnel schéma (Phase E.1 — à créer si MH valide)
# Lister DBs : /var/www/planora-data/calendar360.db + tenants/*.db
```

